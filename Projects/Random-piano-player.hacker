use Hacker::Synth::Sun;
use Hacker::Mixer;

my @seq = map {int rand(24)} 1..64;

my (@c1, @c2);

for (@seq) {
	push @c1, Hacker::Synth::Sun->signal($_, 0.5);
	push @c2, Hacker::Synth::Sun->signal($_+ 12, 0.5);
}

my $mixer = new Hacker::Mixer(channels => [\@c1, \@c2]);
$mixer->master;
