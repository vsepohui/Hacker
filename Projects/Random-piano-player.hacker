use Hacker::Synth::Sin;

my (@c1, @c2);
for (map {int rand(24)} 1..64) {
	push @c1, Hacker::Synth::Sin->signal($_, 0.5);
	push @c2, Hacker::Synth::Sin->signal($_+ 12, 0.5);
}

$h->mixer(\@c1, \@c2)->master;
